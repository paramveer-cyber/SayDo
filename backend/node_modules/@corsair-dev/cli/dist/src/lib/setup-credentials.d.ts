import type { SetupCredentials } from 'corsair/setup';
/**
 * Parses `corsair setup --<plugin> field=value ...` from argv (after "setup").
 * Supports `--plugin=<id>`, `--plugin <id>`, and `--<pluginId>` plugin selectors.
 */
export declare function parseSetupCredentials(rawArgs: readonly string[], pluginIds: readonly string[]): SetupCredentials;
export declare function getSetupRawArgs(argv?: string[]): string[];
//# sourceMappingURL=setup-credentials.d.ts.map